typedef struct node *Node;

void emptyNode(void);

void setKey(char newKey, Node list);

void setValue(char newVal, Node list);

void nextEntry(Node head, Node tail);

char listKey(Node list);

char listValue(Node list);
